{{
  "deploy": {
    "startCommand": "node server.js",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  }
}
  "name": "nafas",
  "version": "1.0.0",
  "description": "نَفَس",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "node-fetch": "2.6.9"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
